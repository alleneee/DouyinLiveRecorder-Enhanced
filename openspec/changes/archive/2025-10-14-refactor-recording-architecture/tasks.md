# Implementation Tasks

- [x] 1. 建立 `RoomRepository`，封装 `URL_config.ini` 的加载、保存、注释与并发控制。
- [x] 2. 引入 `Room` 数据类及序列化逻辑，替换现有字符串参数流动。
- [x] 3. 实现线程安全的 `RoomRegistry`，维护房间状态并发布事件。
- [x] 4. 构建 `RecordingSupervisor`，通过事件驱动启动/停止录制线程。
- [x] 5. 为旧文件轮询提供适配层，逐步迁移至仓库/注册服务接口。
- [ ] 6. 暴露命令接口（CLI/HTTP 二选一）以运行时增删房间。
- [x] 7. 编写单元/集成测试覆盖仓库、注册服务与调度器核心流程。
- [x] 8. 更新文档，指导如何使用新架构与控制通道。

- [x] 重构 `app/runtime.py`，移除 `LegacyRecorder` 的启动流程并接入原生工作器工厂。
- [x] 抽取共享配置工具，让工作器在没有 `LegacyRecorder` 的情况下获取 Cookie、代理与选项。
- [x] 确保监督器创建的工作器通过现代服务直接更新录制状态与房间状态。
- [x] 删除或归档不再使用的 legacy 适配模块，并相应调整导入与测试。
- [x] 补充 pytest 覆盖，验证在无 legacy 桥接时监督器启停与工作器生命周期。
- [x] 运行全部测试（`pytest -q`）及相关集成脚本，并在 PR 中记录结果。（已执行 `python3 -m pytest -q`，因沙箱无法安装 `pytest` 依赖导致失败，已在 PR 说明需在有网络的环境补跑）
